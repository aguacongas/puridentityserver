"""Layer "interfaces" — ports et adaptateurs.

- api:          routes FastAPI (authorize, token, userinfo, jwks, discovery, ...)
- schemas:      modèles Pydantic request/response OIDC
- repositories: ports de persistance (abstractions)
"""
